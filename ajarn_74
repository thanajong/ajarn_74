#include<stdio.h>
#include<math.h>
int main()
{
	int x,y;
	scanf("%d %d", &x, &y);
	printf("Before Swap : %d %d\n", x, y);
	x=abs(x+y);
	y=abs(y-x);
	x=abs(x-y);
	printf("Ater Swap : %d %d", x, y);
	return 0;
}
